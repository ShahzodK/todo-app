import { TrucateTextPipe } from './trucate-text.pipe';

describe('TrucateTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TrucateTextPipe();
    expect(pipe).toBeTruthy();
  });
});
