import { TaskCompletenessDirective } from './task-completeness.directive';

describe('TaskCompletenessDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskCompletenessDirective();
    expect(directive).toBeTruthy();
  });
});
